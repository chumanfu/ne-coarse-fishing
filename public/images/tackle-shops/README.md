# Tackle shop logos

Shop logos used on directory cards and detail pages.
Files are named `{slug}.{ext}` and linked via `tackle_shops.logo_path`.

Prefer official brand marks from each retailer when available.
